export default class Frog { //8. We know we need a frog for the racing, so let's make that class and export it 
  constructor(data) {
    this.name = data.name
    this.img = data.img
    this.bet = 0
  }
} 